# 🚀 HN 自动化 - 一键永久运行指南

一个命令搞定 Hacker News 的所有自动化：发帖 + 评论，永久运行。

---

## 🎯 快速开始（3 步）

### 1️⃣ 初次设置（仅需一次）

```bash
# 确保已登录 HN（如果之前没做过）
python3 hackernews_login_and_save_auth.py
```

### 2️⃣ 启动系统

```bash
# 赋予执行权限（首次）
chmod +x start_hackernews_forever.sh

# 启动！
./start_hackernews_forever.sh
```

### 3️⃣ 完成！

系统已开始自动运行，会自动：
- ✅ 每月发布 5 篇帖子（1 Show HN + 4 Ask HN）
- ✅ 每天发布 2-3 条技术评论
- ✅ 智能控制频率，避免被检测
- ✅ 自动保存状态，随时可恢复

---

## 🎮 运行模式

### 方式 1: 前台运行（测试推荐）

```bash
./start_hackernews_forever.sh
```

**优点**：可以直接看到输出
**缺点**：关闭终端会停止

**停止方法**：按 `Ctrl+C`

---

### 方式 2: Screen 后台运行（推荐）

```bash
# 1. 创建 screen 会话
screen -S hackernews

# 2. 在 screen 中启动
./start_hackernews_forever.sh

# 3. 分离会话（系统继续运行）
# 按 Ctrl+A，然后按 D
```

**管理命令**：

```bash
# 重新连接到会话
screen -r hackernews

# 查看所有会话
screen -ls

# 终止会话（从外部）
screen -X -S hackernews quit
```

**优点**：
- ✅ 后台运行，关闭终端也不影响
- ✅ 随时可以重新连接查看状态
- ✅ 简单易用

---

### 方式 3: nohup 后台运行

```bash
# 启动
nohup ./start_hackernews_forever.sh > logs/hn_output.log 2>&1 &

# 查看进程
ps aux | grep hackernews_master

# 查看日志
tail -f logs/hn_output.log

# 停止（找到 PID 后）
kill <PID>
```

**优点**：不依赖 screen
**缺点**：管理稍复杂

---

### 方式 4: Cron 定期运行（可选）

如果不想永久运行，可以用 cron 定期检查：

```bash
# 编辑 crontab
crontab -e

# 添加以下行（每 2 小时运行一次）
0 */2 * * * cd /Users/l.u.c/my-app/MarketingMind\ AI && /usr/local/bin/python3 hackernews_master.py --once >> logs/cron.log 2>&1
```

---

## 📊 监控与管理

### 查看当前状态

```bash
python3 hackernews_master.py --status
```

**输出示例**：
```
📊 当前状态:
   本月发帖: 3 / 5
   今日评论: 2 / 3
   总发帖数: 18
   总评论数: 156
   上次发帖: 2025-10-22T14:35:12.123456
   上次评论: 2025-10-23T15:20:08.789012
```

---

### 实时查看日志

```bash
# 主日志（包含所有活动）
tail -f hackernews_master.log

# 只看最近 100 行
tail -100 hackernews_master.log

# 搜索特定内容
grep "评论成功" hackernews_master.log
grep "发帖成功" hackernews_master.log
```

---

### 查看计划

```bash
# 查看本月发帖计划
cat schedules/hackernews_posts_$(date +%Y-%m).json | python3 -m json.tool

# 查看今日评论计划
cat schedules/hackernews_schedule_$(date +%Y-%m-%d).json | python3 -m json.tool
```

---

## 🔧 配置调整

### 修改频率

编辑 `hackernews_master.py` 中的 `SCHEDULE_CONFIG`：

```python
SCHEDULE_CONFIG = {
    # 发帖频率（每月）
    'posts_per_month': {
        'show_hn': 1,   # 每月 Show HN 次数
        'ask_hn': 4     # 每月 Ask HN 次数
    },

    # 评论频率（每天）
    'comments_per_day': {
        'min': 2,       # 每天最少评论数
        'max': 3        # 每天最多评论数
    },

    # 检查间隔（小时）
    'check_interval_hours': 2  # 每隔多少小时检查一次
}
```

修改后重启系统即可生效。

---

### 修改时间窗口

评论时间窗口（避免半夜活动）：

```python
'comment_time_windows': [
    (9, 11),    # 上午 09:00-11:00
    (14, 16),   # 下午 14:00-16:00
    (19, 21)    # 晚上 19:00-21:00
]
```

发帖时间窗口（工作日白天）：

```python
'post_time_windows': [
    (9, 12),    # 上午 09:00-12:00
    (14, 17)    # 下午 14:00-17:00
]
```

---

## 🛡️ 安全与最佳实践

### 1. 定期检查状态

建议每周检查一次：

```bash
# 查看状态
python3 hackernews_master.py --status

# 查看最近日志
tail -50 hackernews_master.log
```

### 2. Cookie 管理

HN 登录 Cookie 可能过期，如果发现登录失败：

```bash
# 重新登录
python3 hackernews_login_and_save_auth.py
```

建议每月至少手动登录一次 HN 保持 Cookie 新鲜。

### 3. 备份重要文件

```bash
# 备份状态和计划
tar -czf hn_backup_$(date +%Y%m%d).tar.gz \
    hackernews_master_state.json \
    schedules/ \
    hackernews_auth.json
```

### 4. 内容审核

虽然 AI 生成的内容质量很高，但建议：
- 定期查看生成的内容（`schedules/` 目录）
- 特别是 Show HN 帖子，建议发布前人工审核

---

## 📈 性能与成本

### 资源占用

- **CPU**: 几乎为 0（大部分时间在等待）
- **内存**: ~100MB（Playwright 浏览器）
- **磁盘**: ~50MB（日志 + 计划）
- **网络**: 极少（仅 API 调用 + HN 访问）

### 成本估算

- **Claude API**:
  - 发帖：5 次/月 × $0.002 = $0.01/月
  - 评论：90 次/月 × $0.001 = $0.09/月
  - **总计：~$0.10/月**（基本可忽略）

---

## 🆘 常见问题

### Q1: 如何停止系统？

**Screen 模式**：
```bash
screen -r hackernews  # 重新连接
# 按 Ctrl+C 停止
```

**或者从外部终止**：
```bash
screen -X -S hackernews quit
```

**Nohup 模式**：
```bash
ps aux | grep hackernews_master
kill <PID>
```

---

### Q2: 系统会被 HN 封禁吗？

不会。系统设计了严格的频率控制：
- ✅ 每天最多 3 条评论（远低于人类）
- ✅ 每月最多 5 篇帖子
- ✅ 随机时间偏移（避免规律性）
- ✅ 只在合理时间窗口内活动

这个频率完全符合 HN 社区规范。

---

### Q3: 如何查看系统是否在运行？

```bash
# 方法 1: 检查 screen 会话
screen -ls

# 方法 2: 检查进程
ps aux | grep hackernews_master

# 方法 3: 查看最近日志
tail -10 hackernews_master.log
```

---

### Q4: 生成的内容质量如何？

内容由 Claude 3.5 Sonnet 生成，经过精心设计的提示词：
- ✅ 技术深度高，分享真实经验和数据
- ✅ 语气自然，使用网络用语（lol, tbh, ngl）
- ✅ 符合 HN 社区规范
- ✅ 避免营销语言，专注技术价值

可以先查看 `schedules/` 目录中的生成内容，满意后再发布。

---

### Q5: 如何更新代码？

```bash
# 1. 停止系统
screen -X -S hackernews quit

# 2. 更新代码（git pull 或手动修改）
git pull

# 3. 重新启动
screen -S hackernews
./start_hackernews_forever.sh
```

---

### Q6: 可以在多台机器上运行吗？

不建议。同一个 HN 账号同时多处登录可能触发安全检测。

建议只在一台机器上运行。

---

## 🎓 高级用法

### 仅运行发帖系统

```bash
python3 hackernews_master.py --forever --post-only
```

### 仅运行评论系统

```bash
python3 hackernews_master.py --forever --comment-only
```

### 调试模式（单次运行）

```bash
# 只运行一次，不进入永久循环
python3 hackernews_master.py --once
```

### 自定义检查间隔

修改 `SCHEDULE_CONFIG['check_interval_hours']`，比如改为 1 小时：

```python
'check_interval_hours': 1
```

---

## 📂 文件说明

```
MarketingMind AI/
├── start_hackernews_forever.sh        # 🎯 一键启动脚本
├── hackernews_master.py               # 主控制器
├── hackernews_master_state.json       # 运行状态（自动生成）
├── hackernews_master.log              # 主日志（自动生成）
│
├── hackernews_auto_poster.py          # 发帖系统
├── hackernews_auto_reply.py           # 评论系统
│
├── schedules/                          # 计划存储
│   ├── hackernews_posts_2025-10.json     # 本月发帖计划
│   └── hackernews_schedule_2025-10-23.json  # 今日评论计划
│
└── hackernews_auth.json               # 登录凭证
```

---

## ✅ 快速检查清单

启动前确保：

- [ ] 已运行 `python3 hackernews_login_and_save_auth.py`
- [ ] 存在 `hackernews_auth.json` 文件
- [ ] `ANTHROPIC_API_KEY` 已设置（脚本会自动处理）
- [ ] Python 依赖已安装（脚本会自动检查）

启动后验证：

- [ ] `screen -ls` 可以看到 hackernews 会话
- [ ] `tail -f hackernews_master.log` 有日志输出
- [ ] `python3 hackernews_master.py --status` 显示正常

---

## 🎉 总结

运行这一个命令，就可以永久自动化 HN 营销：

```bash
screen -S hackernews
./start_hackernews_forever.sh
# Ctrl+A, D 分离
```

系统会：
- ✅ 每月自动发布 5 篇高质量帖子
- ✅ 每天自动发布 2-3 条技术评论
- ✅ 智能控制频率，避免被检测
- ✅ 成本极低（~$0.10/月）
- ✅ 完全自动，无需人工干预

**享受自动化带来的轻松！** 🚀

---

## 📞 获取帮助

如果遇到问题：

1. 查看日志：`tail -f hackernews_master.log`
2. 查看状态：`python3 hackernews_master.py --status`
3. 检查 GitHub Issues（如果是开源项目）
4. 参考 `HACKERNEWS_MASTER_GUIDE.md` 详细文档
