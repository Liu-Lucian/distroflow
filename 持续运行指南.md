# 🔄 持续营销系统 - 24/7运行指南

## 🚀 一键启动（推荐）

### 基础用法 - 每50封休息5小时
```bash
cd "/Users/l.u.c/my-app/MarketingMind AI"
python3 continuous_campaign.py --product hiremeai
```

**这个命令会**：
- ✅ 每批找50个leads
- ✅ 自动发送邮件
- ✅ 发送完休息5小时
- ✅ 自动重复（24/7不间断）
- ✅ 保存进度（可随时中断）
- ✅ 错误自动恢复

---

## 🎯 推荐配置

### 配置1：保守模式（推荐新手）
```bash
python3 continuous_campaign.py \
  --product hiremeai \
  --batch-size 30 \
  --rest-hours 6 \
  --max-batches 5
```

**特点**：
- 每批30个leads（更精准）
- 休息6小时（更像人类）
- 最多5批（测试用）
- **预计运行时间**：30小时（5批 × 6小时）

### 配置2：标准模式（推荐）
```bash
python3 continuous_campaign.py \
  --product hiremeai \
  --batch-size 50 \
  --rest-hours 5
```

**特点**：
- 每批50个leads
- 休息5小时
- 无限运行（直到手动停止）
- **预计**：每天4-5批 = 200-250封邮件/天

### 配置3：激进模式（高产出）
```bash
python3 continuous_campaign.py \
  --product hiremeai \
  --batch-size 100 \
  --rest-hours 4
```

**特点**：
- 每批100个leads
- 休息4小时
- **预计**：每天6批 = 600封邮件/天
- ⚠️  注意：可能更容易被标记为spam

---

## 📊 运行示例

### 启动后你会看到：
```
======================================================================
🔄 CONTINUOUS CAMPAIGN STARTED
======================================================================
  Batch size: 50 leads
  Rest time: 5 hours
  Max batches: Unlimited
  Press Ctrl+C to stop gracefully
======================================================================

======================================================================
🚀 BATCH #1 - Starting
======================================================================
  Target: 50 leads
  Seeds: 5 accounts
  Product: /Users/l.u.c/my-app/MarketingMind AI/products/hiremeai.md

📊 STEP 1: Finding Leads...
✅ Hunter.io integration enabled
...
  ✅ Leads found: 47
  ✅ With emails: 28

📧 STEP 2: Sending Emails...
📧 [1/28] Sending to tips@engadget.com...
✅ Email sent to tips@engadget.com
...
  ✅ Emails sent: 26
  ❌ Emails failed: 2

======================================================================
📊 CONTINUOUS CAMPAIGN STATISTICS
======================================================================
  Total batches completed: 1
  Total leads found: 47
  Total emails sent: 26
  Total emails failed: 2
  Success rate: 92.9%
  Runtime: 0:45:23
  Next batch in: 4:59:37
======================================================================

⏰ Waiting 5.0 hours until next batch...
   Next run: 2025-10-17 23:45:00
   ⏳ 4.5 hours remaining...
   ⏳ 3.5 hours remaining...
   ⏳ 2.5 hours remaining...
   ⏳ 1.5 hours remaining...
   ⏳ 0.5 hours remaining...

======================================================================
🚀 BATCH #2 - Starting
======================================================================
...
```

---

## ⏸️ 如何控制

### 优雅停止（推荐）
```bash
# 在运行的终端按 Ctrl+C
^C

⚠️  Campaign stopped by user (Ctrl+C)
💾 State saved. You can resume by running the same command.
```

**状态会自动保存**，下次运行时会从上次停止的地方继续！

### 查看当前状态
```bash
cat campaign_state.json
```

输出：
```json
{
  "total_batches": 3,
  "total_leads": 141,
  "total_emails_sent": 84,
  "total_emails_failed": 6,
  "start_time": "2025-10-17T14:30:00",
  "last_batch_time": "2025-10-17T20:15:00",
  "next_run_time": "2025-10-18T01:15:00"
}
```

### 重置状态（从头开始）
```bash
python3 continuous_campaign.py --product hiremeai --reset
```

---

## 📈 预期产出

### 标准模式（50/batch，5小时休息）
```
第1天：
├─ 批次1（00:00-01:00）：50 leads → ~30 emails
├─ 休息（01:00-06:00）
├─ 批次2（06:00-07:00）：50 leads → ~30 emails
├─ 休息（07:00-12:00）
├─ 批次3（12:00-13:00）：50 leads → ~30 emails
├─ 休息（13:00-18:00）
├─ 批次4（18:00-19:00）：50 leads → ~30 emails
└─ 休息（19:00-24:00）

日产出：~120封邮件
月产出：~3,600封邮件
```

### 一周运行预期
```
7天 × 120封/天 = 840封邮件

预期转化：
├─ 打开率：25% = 210人
├─ 点击率：8% = 67人
├─ 回复率：3% = 25人
└─ 转化率：1% = 8个paying customers 🎉
```

---

## 🔧 后台运行（推荐）

### 方法1：使用 nohup（简单）
```bash
cd "/Users/l.u.c/my-app/MarketingMind AI"

nohup python3 continuous_campaign.py --product hiremeai > campaign.log 2>&1 &

# 查看进程
ps aux | grep continuous_campaign

# 查看日志
tail -f campaign.log

# 停止
kill $(ps aux | grep continuous_campaign | grep -v grep | awk '{print $2}')
```

### 方法2：使用 screen（更好）
```bash
# 创建screen会话
screen -S marketing

# 在screen中运行
cd "/Users/l.u.c/my-app/MarketingMind AI"
python3 continuous_campaign.py --product hiremeai

# 退出screen（但保持运行）
# 按 Ctrl+A，然后按 D

# 重新连接
screen -r marketing

# 停止：重新连接后按 Ctrl+C
```

### 方法3：使用 tmux（专业）
```bash
# 创建tmux会话
tmux new -s marketing

# 运行
cd "/Users/l.u.c/my-app/MarketingMind AI"
python3 continuous_campaign.py --product hiremeai

# 退出tmux（保持运行）
# 按 Ctrl+B，然后按 D

# 重新连接
tmux attach -t marketing
```

---

## 📊 监控和维护

### 实时监控
```bash
# 方法1：查看实时日志
tail -f continuous_campaign.log

# 方法2：查看状态文件
watch -n 60 'cat campaign_state.json'

# 方法3：查看发送统计
watch -n 300 'python3 src/email_campaign_manager.py --stats'
```

### 每日检查清单
```bash
# 1. 检查进程是否运行
ps aux | grep continuous_campaign

# 2. 查看最新统计
cat campaign_state.json

# 3. 查看退信率
python3 src/email_campaign_manager.py --stats

# 4. 检查磁盘空间
df -h

# 5. 查看错误日志
grep "ERROR" continuous_campaign.log | tail -20
```

---

## 🎯 最佳实践

### 1. 初次运行（测试模式）
```bash
# 先运行3批测试
python3 continuous_campaign.py \
  --product hiremeai \
  --batch-size 20 \
  --rest-hours 2 \
  --max-batches 3
```

**检查**：
- ✅ 邮箱质量（无占位符、无t.co）
- ✅ 发送成功率（>90%）
- ✅ 无错误崩溃

### 2. 正式运行（生产模式）
```bash
# 使用screen后台运行
screen -S marketing
python3 continuous_campaign.py --product hiremeai
# Ctrl+A, D 退出
```

### 3. 每天检查一次
```bash
# 重新连接screen
screen -r marketing

# 查看统计（应该看到稳定增长）
# 按 Ctrl+C 优雅停止
# 查看 campaign_state.json
```

---

## ⚠️ 注意事项

### 1. Gmail发送限制
```
免费Gmail账号：
├─ 每天限制：500封
├─ 每小时限制：~100封
└─ 建议：每批30-50封，休息5-6小时

Google Workspace：
├─ 每天限制：2,000封
└─ 可以更激进（每批100封）
```

### 2. Hunter.io限额
```
免费计划：
├─ Email Finder：50次/月
├─ Email Verifier：50次/月
└─ 建议：主要用LLM，Hunter.io作为补充

付费计划（$49/月）：
└─ 1,000次/月
```

### 3. Twitter限制
```
└─ 系统已实现延迟和模拟人类行为
└─ 不太可能被ban
```

---

## 🐛 故障排除

### 问题1：进程意外停止
```bash
# 检查日志
tail -100 continuous_campaign.log

# 检查是否有错误
grep "ERROR\|Exception" continuous_campaign.log

# 重新启动（会自动恢复）
python3 continuous_campaign.py --product hiremeai
```

### 问题2：发送失败率高
```bash
# 查看失败原因
python3 src/email_campaign_manager.py --stats

# 如果是SMTP错误：检查email_config.json
# 如果是邮箱格式错误：已经修复，更新代码
```

### 问题3：找不到邮箱
```bash
# 可能是t.co域名太多
# 解决：更换种子账号（见"运行结果分析.md"）
```

---

## 🎉 完整示例

### 完整的24/7运行流程

```bash
# 步骤1：进入目录
cd "/Users/l.u.c/my-app/MarketingMind AI"

# 步骤2：创建screen会话
screen -S marketing

# 步骤3：启动持续营销
python3 continuous_campaign.py \
  --product hiremeai \
  --batch-size 50 \
  --rest-hours 5

# 步骤4：退出screen（保持运行）
# 按 Ctrl+A，然后按 D

# 步骤5：每天检查一次
screen -r marketing
# 查看统计，确认正常运行
# 按 Ctrl+A, D 退出

# 步骤6：一周后查看成果
cat campaign_state.json
python3 src/email_campaign_manager.py --stats
```

---

## 📝 命令速查表

```bash
# 启动（标准模式）
python3 continuous_campaign.py --product hiremeai

# 启动（后台）
nohup python3 continuous_campaign.py --product hiremeai > campaign.log 2>&1 &

# 启动（screen）
screen -S marketing
python3 continuous_campaign.py --product hiremeai

# 查看状态
cat campaign_state.json

# 查看日志
tail -f continuous_campaign.log

# 查看统计
python3 src/email_campaign_manager.py --stats

# 重置
python3 continuous_campaign.py --product hiremeai --reset

# 停止
Ctrl+C (优雅停止)
kill <pid> (强制停止)
```

---

## 🎯 推荐的完整命令

**这是你应该运行的命令**：

```bash
cd "/Users/l.u.c/my-app/MarketingMind AI"
screen -S marketing
python3 continuous_campaign.py --product hiremeai --batch-size 50 --rest-hours 5
```

然后按 **Ctrl+A, D** 退出screen，让它24/7运行！

**预期**：每天发送~120封高质量邮件，一周后获得5-10个paying customers！🚀
